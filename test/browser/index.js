import { h, render, rerender } from 'preact';
import App from 'components/app';
import 'style';

/*global sinon,expect*/

describe('App', () => {
	it('should work', () => {
		expect(true).to.be.true;
	});
});
